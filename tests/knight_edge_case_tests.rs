//! Knight move decoder edge case tests (legacy decoder removed)

#[cfg(test)]
mod tests {
    #[test]
    fn legacy_knight_decoder_removed_placeholder() {
        // Legacy knight decoder was removed per Task 10.
        // Placeholder test to keep suite compiling until SG4Parser-based tests are added.
        assert!(true);
    }
}
